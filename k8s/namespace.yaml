# ─────────────────────────────────────────────────────────────────────────────
# namespace.yaml — llmqa namespace
#
# All LLM-QA-OPS resources live in this namespace for clean isolation.
# Apply first: kubectl apply -f k8s/namespace.yaml
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Namespace
metadata:
  name: llmqa
  labels:
    app.kubernetes.io/part-of: llm-qa-ops
    app.kubernetes.io/managed-by: kubectl
